
  func 按某项生成映射(列表, 生成键, 生成值) {
    点表 = {:}
    for 项 in 列表 {
      键 = 生成键(项)
      值 = 生成值(项)
      if 点表.get(键) {
        点表[键].append(值)
      } else {
        点表[键] = [值]
      }
    }
    return 点表
  }