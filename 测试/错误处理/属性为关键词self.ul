type Person {
    func $Person(name) {
        self.while = name
    }
}
mulan = Person(1)
print(mulan.name)
/*
分析器.错误.语法错误: 文件 "测试/错误处理/属性为关键词.ul", 第3行, 第14列, 没认出这个词 "while"
        self.✋while = name
*/