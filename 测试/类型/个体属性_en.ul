type Person {
    func $Person(name) {
        self.name = name
        $nickname = name
        $type = name
    }
}
mulan = Person(1)
print(mulan.name)
print(mulan.nickname)
print(mulan.__getattribute__('type'))
